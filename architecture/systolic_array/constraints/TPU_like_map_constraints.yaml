# For simplicity, no map space constraint is defined
mapspace_constraints:
  targets:
    
  #- target: shared_glb
    #type: spatial
    #split: 1
    #permutation: CM

  - target: shared_glb
    type: temporal
    factors: N=1 P=1 Q=1

  - target: weights_spad
    type: temporal
    factors: N=1 P=1 Q=1
